# Model weights (commitujeme jen kód, ne binární váhy)
*.pth
*.bin
*.onnx
*.pb
*.pt
*.h5
*.keras
models/vitpose/vitpose_huge.pth
best_lstm_behavior_model/

# Other large files
*.tar.gz
*.zip

# Python cache + temp + IDE
__pycache__/
*.pyc
*.bak
nul
.idea/
logs/

# External datasets (download separately, viz data/README.md) — příliš velké pro git
data/
# Note: data/AVA/annotations/ JE commited (jen ~50 MB CSV) — pokud potřeba, force-add ručně

# Pozn.: demo_outputs/ (anotovaná videa) a reference_img/ JSOU commitnuté (ukázky pro BP)